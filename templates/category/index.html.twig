{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block stylesheets %} 
<link rel="stylesheet" href ={{asset("css/category.css")}}>
{% endblock %}

{% block body %}
  <h1 class="titre"> Nos Catégories</h1>
    
    {# Filtrer les catégories par nom #}
    {# <form action="{{path('app_category')}}" method="GET">
        <input type="text" name="filter" placeholder="Filtrer par nom">
        <button type="submit">Filtrer</button>
    </form> #}
    
    <ul class="cards">

        <li class="cards-item">
            <div class="card">
                <div class="card-image card-image2"></div>
                    <div class="card-content">
                    <div class="card-title">Burger au poulet </div>
                    <p class="card-text">This defines the ability for a flex item to grow if necessary. It accepts a unitless value that serves as a proportion. It dictates what amount of the available space inside the flex container the item should take up.</p>
                    <a href="{{path('app_product_vu_client')}}" class="cards-button button-block"><span>voir catalogue</span></a>
                </div>
            </div>
        </li>

        
        <li class="cards-item">
            <div class="card">
                <div class="card-image card-image3"></div>
                    <div class="card-content">
                    <div class="card-title">Burger au Boeuf</div>
                    <p class="card-text">This defines the ability for a flex item to grow if necessary. It accepts a unitless value that serves as a proportion. It dictates what amount of the available space inside the flex container the item should take up.</p>
                    <a href="{{path('app_product_vu_client')}}" class="cards-button button-block"><span>voir catalogue</span></a>
                </div>
            </div>
        </li>

        
        <li class="cards-item">
            <div class="card">
                <div class="card-image card-image4"></div>
                    <div class="card-content">
                    <div class="card-title">Avec Bacon </div>
                    <p class="card-text">This defines the ability for a flex item to grow if necessary. It accepts a unitless value that serves as a proportion. It dictates what amount of the available space inside the flex container the item should take up.</p>
                    <a href="{{path('app_product_vu_client')}}" class="cards-button button-block"><span>voir catalogue</span></a>
                </div>
            </div>
        </li>

        
        <li class="cards-item">
            <div class="card">
                <div class="card-image card-image5"></div>
                    <div class="card-content">
                    <div class="card-title">Extra</div>
                    <p class="card-text">This defines the ability for a flex item to grow if necessary. It accepts a unitless value that serves as a proportion. It dictates what amount of the available space inside the flex container the item should take up.</p>
                    <a href="{{path('app_product_vu_client')}}" class="cards-button button-block"><span>voir catalogue</span></a>
                </div>
            </div>
        </li>

        
        <li class="cards-item">
            <div class="card">
                <div class="card-image card-image7"></div>
                    <div class="card-content">
                    <div class="card-title">Golden Menu </div>
                    <p class="card-text">This defines the ability for a flex item to grow if necessary. It accepts a unitless value that serves as a proportion. It dictates what amount of the available space inside the flex container the item should take up.</p>
                    <a href="{{path('app_product_vu_client')}}" class="cards-button button-block"><span>voir catalogue</span></a>
                </div>
            </div>
        </li>
    

    </ul>

{% endblock %}
